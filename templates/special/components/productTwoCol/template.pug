.container
	.row(v-for="items in cmsList")
		.pro-area(v-for="(item, index) in items", @click="product(item.product_id, type ? type[index] : 'product')")
			image.pro-img(:src="item.image")
			text.pro-info {{item.name}}
			text.pro-price ￥{{item.price}}